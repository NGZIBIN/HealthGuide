<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.healthguide">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".workoutChest"></activity>
        <activity android:name=".workoutleg" />
        <activity android:name=".workoutArm" />
        <activity android:name=".activityWorkoutFullbody" />
        <activity android:name=".dietBodyForth" />
        <activity android:name=".dietBodyThird" />
        <activity android:name=".dietBodySecond" />
        <activity android:name=".dietBodyFirst" />
        <activity android:name=".diet_Activity" />
        <activity android:name=".workout_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>